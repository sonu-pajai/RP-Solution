<% content_for :breadcrumb do %>
  <%= link_to "Dashboard", root_path %><span class="separator">›</span> <%= link_to "Transactions", transactions_path %><span class="separator">›</span> Bulk Upload
<% end %>

<div class="page-header">
  <h2>Bulk Upload - Transactions</h2>
  <%= link_to "← Back", transactions_path, class: "action-link" %>
</div>

<div class="card">
  <div class="info-box">
    <p class="info-box-title">📋 File Format</p>
    <p class="info-box-text">
      Upload a CSV file with these columns:<br>
      <code>Nature | Transaction Type | Sub Type | AS-18 | ACB | SEBI</code>
    </p>
  </div>

  <div class="mb-24">
    <%= link_to template_transactions_path, class: "sample-download" do %>
      <span class="sample-download-icon">📥</span>
      <span><strong>Download Template</strong><br><span class="sample-download-subtitle">CSV template with correct headers</span></span>
    <% end %>
  </div>

  <%= form_with(url: bulk_upload_transactions_path, method: :post, multipart: true) do |f| %>
    <div class="form-group">
      <label>Select File</label>
      <%= f.file_field :file, accept: ".csv", class: "form-control" %>
      <span class="form-hint">Accepted: .csv</span>
    </div>

    <div class="flex-row">
      <%= f.submit "⬆ Upload", class: "btn-primary btn-lg" %>
      <%= link_to "Cancel", transactions_path, class: "btn-secondary" %>
    </div>
  <% end %>
</div>
